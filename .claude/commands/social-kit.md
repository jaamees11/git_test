Generate today's social posts for the user to paste to X, LinkedIn, and Reddit.

Steps:

1. Check if the latest `.cache/issue-*.json` exists (from today's issue).
   - If yes, use that as the source of items.
   - If no, run `python -m pipeline.run --collect-only` first to get today's items.
2. Check `.cache/share-x-*.md`, `.cache/share-linkedin-*.md`, `.cache/share-reddit-*.md` — these are auto-generated by the publisher every time an issue ships. If they exist for today, just point the user at those files.
3. If they don't exist, or the user wants fresh angles:
   - Read today's `.cache/issue-*.json` (or draft JSON)
   - Write 3 paste-ready social posts (X ≤ 270 chars; LinkedIn long-form; Reddit with subreddit suggestions)
   - Save them as `.cache/social-manual-YYYYMMDD.md`
4. For Reddit specifically, also suggest which sub is best for today's items based on the content mix (e.g. lots of dev tools → r/LocalLLaMA; lots of product launches → r/SideProject).
5. Remind the user not to cross-post the same Reddit post to 3+ subs on the same day (spam filter).
